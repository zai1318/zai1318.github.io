# Zohaib Khan — Academic Website

Built with the [Minimal Mistakes Jekyll theme](https://mmistakes.github.io/minimal-mistakes/).

## 🚀 Deploy

1. Place these files in your `zai1318.github.io` folder.  
2. Run:
   ```bash
   git add .
   git commit -m "Add academic website"
   git push origin main
